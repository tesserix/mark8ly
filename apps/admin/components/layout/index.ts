// Barrel for the admin layout primitives. Re-export everything so pages
// only need one import line: `import { AdminPage, PageSection, ... } from "@/components/layout"`.

export { AdminPage, PageSection, ReadOnlyNotice } from "./AdminPage";
export { PageSkeleton } from "./PageSkeleton";
export { ErrorState } from "./ErrorState";
