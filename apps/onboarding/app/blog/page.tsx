import { StubPage } from "@/components/marketing/StubPage";

export const metadata = { title: "Blog — Mark8ly" };

export default function BlogPage() {
  return (
    <StubPage
      title="The Mark8ly Blog"
      description="Stories, tips, and behind-the-scenes from people building their dream stores."
    />
  );
}
