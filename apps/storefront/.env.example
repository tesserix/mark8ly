# Storefront environment variables
NODE_ENV=development
PLATFORM_API_URL=http://localhost:8086
# Dev-only: when the host isn't a proper {slug}.mark8ly.com
# subdomain, the home page falls back to this slug so bare
# localhost:4203 renders a real store during local iteration.
DEFAULT_STORE_SLUG=
